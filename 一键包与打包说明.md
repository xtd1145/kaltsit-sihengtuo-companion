# 一键包 / 打包说明

## Windows 一键包（免安装）
- `凯尔希-思衡托桌宠-Windows一键包-v0.1.0.zip`：解压后双击 `凯尔希-思衡托桌宠.exe` 即可使用。
- 或运行 `scripts\build-win.ps1` 自行打出 NSIS 安装包（Setup-*.exe）。

## macOS
- macOS 的 .app / .dmg 只能在 macOS 上打包（Windows 无法交叉编译）。
- 在 Mac 上：双击 `scripts/build-mac.command`（或终端执行），产物在 `dist/`。

## 发布自动更新
1. 修改 `package.json` 的 version（如 0.1.1）。
2. Windows：`npm run dist`；macOS：`npm run dist:mac`。
3. 推送代码并打 tag `v0.1.1`，把 Setup-*.exe / *.dmg 上传到对应 GitHub Release。
4. 桌宠「设置 → 更新」填 `xtd1145/kaltsit-sihengtuo-companion` 即可自动发现更新。
